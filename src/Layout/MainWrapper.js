import styled from 'styled-components'

const MainWrapper = styled.div`
  max-width: calc(1200 + 16px * 2);
  margin: 0 auto;
  display: flex;
  min-height: 100%;
  padding: 0 16px;
  flex-direction: column;
`
export default MainWrapper
